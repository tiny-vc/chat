# 登录与启动体验整理

日期：2026-09-03。

## 范围

- 用户暂时没有名称偏好，保留 Chat 为开发占位名称；Dart 侧名称集中于 AppIdentity，iOS / Android 显示名称同步为 Chat，应用 ID 不变。
- 欢迎展示与登录表单合一，采用可滚动、最大宽度受限的布局，使用主题色适配明暗主题。BrandHeader 中的 Material 图标仅为临时视觉标识，不是最终 Logo。
- 注册增加确认密码、用户名格式和昵称长度校验；完善自动填充、密码显隐说明、键盘提交、切换模式清除密码和错误提示。
- 认证控制器阻止重复提交，避免销毁后通知；错误文案不直接展示服务端内部信息。
- 恢复登录期间显示明确的等待状态；业务 API 请求增加连接、发送和接收超时，不改变对象存储上传客户端。
- iOS 麦克风权限说明包含语音消息录制。

## 自动检查

- flutter analyze：无问题。
- flutter test test：65 项通过，包含请求并发保护、销毁后完成、安全错误提示、320px / 1.5 倍字体下的明暗主题注册校验，以及登录恢复期间不提前显示登录页。
- 真实服务集成测试入口：integration_test/auth_entry_flow_test.dart。使用专用测试账号和内存 TokenStore，不把测试凭据写入正常应用。
- iPhone 17 Pro 模拟器真实服务测试通过：键盘提交登录、进入首页、调用退出并返回登录页、令牌清空、密码清空。测试没有点击设置页的退出按钮，不将其计作该按钮交互验收。
- 退出后登录页截图：`/tmp/chat-auth-entry.png`，已检查布局无明显遮挡或溢出。

## 未覆盖与后续项

- 最终名称、原创 Logo、原生启动画面、桌面图标尚未定稿或替换。
- 正式隐私政策、用户协议和在线反馈渠道需另行完善，未添加虚假占位同意项。
- 本次不等于完成所有第一阶段功能；不包含 Android 真机、音视频双向媒体、弱网和发布验收。

## 后续：关于与使用说明

- 登录页和个人页新增入口；共享临时品牌展示，说明麦克风、摄像头、系统文件选择用途。
- 明确标注开发版本及正式条款尚未提供，不把开发说明作为隐私政策。
- 提供反馈模板复制，不自动发送、不采集日志；模板只包含应用名称、平台和待填写字段，不包含账号、接口地址或令牌。未配置客服地址，不提供无效联系按钮。
- 使用 Flutter 内置 LicensePage 展示已登记的开源许可；发布前仍需核对原生与其他第三方组件的许可完整性。
- flutter analyze 无问题；全量 68 项测试通过，新增登录前进入/返回、320px / 2 倍字体明暗主题滚动、复制与许可跳转验证。本轮未做该页面的真机视觉验收。
- 本轮未修改桌面图标或原生启动画面。

后续已接入临时原生图标和启动资源，见 native-brand-assets.md；最终品牌仍未定稿。
